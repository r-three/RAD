from setuptools import setup, find_packages

setup(
    name="reward_augmented_decoding",
    version="0.0.1",
    python_requires=">=3.8",
    author="Haikang Deng",
    long_description="",
    install_requires=[],
    packages = find_packages(),
)